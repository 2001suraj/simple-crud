import 'package:flutter/material.dart';
class AttractionPage1 extends StatelessWidget {
  const AttractionPage1({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}